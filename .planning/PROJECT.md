# Inspatch

## What This Is

A Chrome extension development tool that lets developers visually select elements on locally-served web pages, describe desired changes in natural language, and have those changes automatically applied to the source code via Claude Code CLI. Inspatch bridges the gap between what you see in the browser and the source code that produces it — point, describe, done.

## Core Value

Developers can click any element on their local dev server page, describe what they want changed in plain language, and see the source code updated automatically — eliminating the "find the right file and line" friction entirely.

## Requirements

### Validated

(None yet — ship to validate)

### Active

- [ ] Element selection and inspection on local dev server pages
- [ ] Source Map-based source code location resolution
- [ ] Extension sidebar with element info display and change input
- [ ] Element screenshot capture for visual context
- [ ] WebSocket communication between extension and local server
- [ ] Local middleware server that receives requests and invokes Claude Code CLI
- [ ] Claude Code CLI integration for automated source code changes
- [ ] Auto-apply changes with hot-reload feedback
- [ ] React / Next.js component tree awareness

### Out of Scope

- Vue / Nuxt / Angular support — React-first for v1, other frameworks later
- Mobile browser support — desktop Chrome only
- Real-time collaborative editing — single developer workflow for v1
- Direct Anthropic API integration — using Claude Code CLI, not raw API
- Cloud deployment — local-only tool for development environments
- Undo/rollback UI — rely on git for version control

## Context

Inspatch is a greenfield project targeting frontend developers who work with React/Next.js applications. The core insight is that developers spend significant time context-switching between what they see in the browser and the corresponding source code. By leveraging Source Maps (already generated by modern build tools like Vite, webpack, Next.js), the extension can trace rendered DOM elements back to their original source file and line number.

The architecture has three components:
1. **Chrome Extension** — Content script handles element selection, highlighting, Source Map parsing, and screenshot capture. The sidebar panel provides element info display and natural language input.
2. **Local WebSocket Server** — A lightweight Node.js server that receives structured requests from the extension and orchestrates Claude Code CLI invocations.
3. **Claude Code CLI** — Receives prompts containing element context (component name, file path, line number, current styles, screenshot) and the user's change description, then directly modifies the source files.

The tool relies on dev servers with hot-reload capability (Vite, Next.js dev, Create React App) to provide instant visual feedback after source code changes.

## Constraints

- **Platform**: Chrome extension (Manifest V3) — Chrome Web Store compatibility
- **Source Map dependency**: Requires dev server to generate accessible Source Maps (most modern tools do by default)
- **Local only**: Extension communicates only with localhost — no external network calls
- **Claude Code CLI**: Requires Claude Code to be installed and authenticated on the developer's machine
- **React focus**: v1 component detection targets React's fiber tree and JSX patterns

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| WebSocket for extension ↔ server communication | Real-time bidirectional communication needed for status updates during AI processing | — Pending |
| Claude Code CLI over MCP/API | Simplest integration path; CLI handles file access and authentication natively | — Pending |
| Source Map parsing for code location | Standard mechanism supported by all modern build tools; no build plugin required | — Pending |
| React-first approach | Most popular framework; well-defined component tree via fiber; clear extension path to Vue/others later | — Pending |
| Extension sidebar for input (not inline popup) | More space for element info, input, and status; standard extension UX pattern | — Pending |
| Bun over pnpm | Faster installs, built-in test runner (bun:test), native workspace/monorepo support, simpler toolchain | — Pending |
| WXT 0.20.20 for extension framework | Vite 8-based, file-based entrypoints, MV3 native, HMR across all contexts; beats Plasmo and CRXJS | — Pending |
| source-map-js for Source Map parsing | Synchronous API, no WASM dependency, works cleanly in content script context | — Pending |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-04-13 after initialization*
